# Local / test dashboard (Vite). Copy to .env.development if missing.
# Production builds use CapRover/Vite production env — do not put secrets here.

VITE_API_URL=http://localhost:5001
VITE_WS_URL=ws://localhost:5001
# Deprecated: do not point at the Python engine. Browser → Node only.
# Python engine is internal; browser requests go through VITE_API_URL only.
VITE_LANDING_URL=http://localhost:3001
VITE_TELEGRAM_URL=https://t.me/VCL4XAlphaAccess
VITE_ALLOW_MOCK_AUTH=true
VITE_MOCK_ADMIN_EMAIL=admin@example.test
VITE_MOCK_ADMIN_PASSWORD=change_me_for_local_test
VITE_MOCK_USER_EMAIL=trader@example.test
VITE_MOCK_USER_PASSWORD=change_me_for_local_test
